
namespace Riddle {
    public enum RoundPresenterState {
        PreStart,
        Start,
        DropCards,
        ShowRiddle,
        FlipCards,

        CollectForPresentation,

        End
    }
}
