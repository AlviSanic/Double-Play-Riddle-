# DevLog

This file tracks project changes and updates.

## Entries

### March 27, 2026
- Created `devlog.md` to record development updates.
- Replaced the active `RiddleIdText` display behavior with `DpLogo` while keeping `RiddleIdText` disabled for future reuse.
- Wired `RiddleSceneManager` to use `riddleIdDisplayOverride` (assigned to `DpLogo`) so it shows in sync with the existing loading/tutorial UI timing.
